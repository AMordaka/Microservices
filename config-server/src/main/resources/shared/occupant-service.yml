server:
  port: 8091

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${DB:jdbc:postgresql://localhost:5432/housingdb}
    username: dmcs
    password: docker
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update